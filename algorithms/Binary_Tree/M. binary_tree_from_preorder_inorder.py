# Reconstruct a binary tree from traversal data

def binary_tree_from_preorder_inorder.py(preorder, inorder):      # Time: O(h), hash table: O(n), recursive reconstruction: O(1); space: O(n+h)= O(n) 
  ...
